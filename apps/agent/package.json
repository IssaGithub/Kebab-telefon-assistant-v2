{
  "name": "@restaurant-ai/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set -a && . ../../.env && set +a && tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "set -a && . ../../.env && set +a && node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@restaurant-ai/shared": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
