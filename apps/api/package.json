{
  "name": "@restaurant-ai/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "set -a && . ../../.env && set +a && tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "set -a && . ../../.env && set +a && node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@restaurant-ai/db": "*",
    "@restaurant-ai/shared": "*",
    "fastify": "^5.1.0",
    "livekit-server-sdk": "^2.13.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
